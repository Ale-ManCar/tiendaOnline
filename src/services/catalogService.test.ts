import{afterEach,describe,expect,it,vi}from'vitest';import{fetchStorefrontCatalog}from'./catalogService';
const response=(body:unknown)=>new Response(JSON.stringify(body),{status:200,headers:{'Content-Type':'application/json'}});
describe('catalog API client',()=>{afterEach(()=>vi.unstubAllGlobals());it('maps API categories and products to storefront models',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValueOnce(response([{id:'c1',name:'Home',slug:'home',active:true,createdAt:'2026-01-01',productCount:1}])).mockResolvedValueOnce(response({data:[{id:'p1',name:'Lamp',description:'Warm lamp',category:{id:'c1',name:'Home'},price:39.5,stock:4,image:'https://example.com/lamp.jpg',featured:true,sku:'LAMP-1',createdAt:'2026-01-01',updatedAt:'2026-01-01'}],meta:{page:1,limit:100,total:1,totalPages:1}})));const catalog=await fetchStorefrontCatalog();expect(catalog.products[0]).toMatchObject({id:'p1',category:'Home',active:true,price:39.5});expect(catalog.categories[0].slug).toBe('home')})});
