import{Component,type ReactNode}from'react';export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}render(){return this.state.failed?<main className="container not-found"><h1>Algo salió mal</h1><button className="button primary" onClick={()=>location.reload()}>Recargar</button></main>:this.props.children}}
