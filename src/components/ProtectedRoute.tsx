import type{ReactNode}from'react';import{Navigate}from'react-router-dom';import{useStore}from'../context/StoreContext';export function ProtectedRoute({children,admin=false}:{children:ReactNode;admin?:boolean}){const{currentUser,authReady}=useStore();if(!authReady)return <div className="container not-found" role="status">Verifying session…</div>;if(!currentUser)return <Navigate to="/" replace/>;if(admin&&currentUser.role!=='admin')return <Navigate to="/404" replace/>;return children}
