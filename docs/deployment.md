# Despliegue

El workflow de GitHub Actions instala con `npm ci`, ejecuta pruebas, construye y publica `dist`. El repositorio debe habilitar GitHub Pages con GitHub Actions como origen. Se conserva `/tiendaOnline/` y navegación hash.
